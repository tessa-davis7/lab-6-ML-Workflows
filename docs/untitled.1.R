project:
  title:"csu-ess-lab6"
  author: "Tessa Davis"
  output-dir: docs
  type: website
format:
  html:
    self-contained: TRUE

##Questions
 # 1: What does zero_q_freq represent?
  # The frequency of days wit zero percipitation measured in mm/day
 # 2: Make 2 maps of the sites by aridty and p_mean
 # 3: Build a xgboost (engine) regression (mode) model using boost_tree Build a neural network model using the nnet engine from the baguette package using the bag_mlp function Add this to the above workflow Evaluate the model and compare it to the linear and random forest models Which of the 4 models would you move forward with?